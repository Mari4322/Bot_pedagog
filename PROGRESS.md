# Статус разработки бота «Педагог»

## Этап 1 — Выполнено ✅

### Структура и база данных
- Структура проекта (`teacher_bot/` с подпапками)
- БД SQLite: все таблицы (`users`, `children`, `hobbies`, `subscriptions`, `requests_log`, `settings`)
- Все запросы к БД (`queries.py`)
- Режим записи: `journal_mode = WAL`, `synchronous = FULL` (защита от потери данных)

### Диалог пользователя
- Шаги 1–6: сбор данных (ребёнок, возраст, увлечение, тема, тревожность, резюме) с навигацией «Назад»
- Шаг 7: генерация ответа через polza.ai, счётчик `daily_count`, запись в `requests_log`
- Шаг 8: перегенерация — выбор нового хобби + комментарий, тема берётся из предыдущего запроса
- Шаг 9: новый запрос — полный сброс FSM и старт с шага 1

### Команды пользователя
- `/start` — приветствие (первый / повторный визит), переход к шагу 1
- `/help` — инструкция со списком команд и описанием шагов
- `/cabinet` — личный кабинет: тариф, лимиты, дата следующего платежа, статус
- `/pay` — заглушка (реализация в Этапе 2)
- `/cancel_sub` — заглушка (реализация в Этапе 2)
- Регистрация команд в меню Telegram через `set_my_commands()`

### Интеграция polza.ai
- Подключение через OpenAI SDK с подменённым `base_url`
- Обработка всех кодов ошибок по ТЗ:
  - 401 / 402 → лог + уведомление администратору
  - 408 → лог (таймаут)
  - 429 → retry через 5 сек (до 3 попыток)
  - 500 → лог + уведомление администратору
  - 502 / 503 → retry через 10 сек (до 3 попыток)
  - Все прочие → лог + уведомление администратору
- Пользователь при любой ошибке видит только вежливое извинение
- Счётчик `daily_count` не увеличивается при ошибках
- Автоматический fallback на `gpt-4o` при ошибке 400 (модель не найдена)

### Планировщик APScheduler (UTC+7)
- 00:00 — сброс `daily_count = 0` для всех пользователей
- 00:01 — деактивация подписок, у которых `next_payment_at < now` и `auto_renew = False`
- 00:02 — напоминание пользователям, у которых подписка истекает через ~1 день
- Каждый час — проверка баланса polza.ai с предупреждением администратору при низком балансе

### Панель администратора
- `/admin` — inline-меню с шестью кнопками
- `/change_model` — смена активной нейросети глобально (из списка `AVAILABLE_MODELS`)
- `/get_users` — выгрузка CSV всех пользователей
- `/get_children` — выгрузка CSV детей и увлечений
- `/get_logs` — выгрузка CSV всех запросов и ответов
- `/add_admin <tg_id>` — добавить администратора (команда и кнопка с FSM-вводом)
- `/delete_admin <tg_id>` — удалить администратора (с защитой от самоудаления)
- `/balance` — текущий баланс polza.ai с индикатором статуса

### Безопасность и защита
- Боты-аккаунты игнорируются на уровне `/start` (не регистрируются в БД)
- Боты-аккаунты заблокированы на уровне генерации (`_do_generate`)
- Проверка лимита запросов перед отправкой в нейросеть
- Защита от самоудаления у команды `/delete_admin`

---

## Этап 1.5 — Исправления и настройка сервера ✅

### Исправление потери данных
- Исправлен `DB_PATH` в `.env`: заменён Windows-путь на абсолютный Linux-путь (`/home/bot/teacher_bot/database/bot.db`)
- БД перенесена в правильную папку `database/`
- Изменён `synchronous = NORMAL` → `synchronous = FULL` в `db.py` (немедленная запись на диск)

### Исправления в диалоге
- Добавлен FSM-стейт `hobby_pick` в `states.py` — текст без нажатия кнопки «+ Добавить увлечение» не сохраняется как хобби
- Добавлена фильтрация команд (`/cabinet`, `/pay`, `/help` и др.) во всех текстовых хендлерах — команды больше не записываются как тема или хобби

### Промпт
- Обновлён `prompt.txt` — новый промпт от заказчика (5 шагов, формат без markdown, лимит 4000 символов)

### Бэкапы (временное решение — crontab)
- Ежечасный бэкап: `bot_hourly_00.db` … `bot_hourly_23.db` (24 файла, перезаписываются каждый день)
- Ежедневный бэкап: `bot_daily_Mon.db` … `bot_daily_Sun.db` (7 файлов, перезаписываются каждую неделю)
- Хранятся в `/home/bot/backups/`

### Настройка сервера
- Systemd-сервис `teacher-bot.service` с `Restart=always`
- Код синхронизирован между сервером и GitHub

---

## Этап 2 — Выполнено ✅

### Тарифы (`tariffs.py`)
- Новый файл с описанием всех тарифов (тестовые цены):
  - `free` — Пробный: 10 генераций, бесплатно
  - `basic` — Базовый: безлимит, 1 руб / 30 дней
  - `premium` — Премиум: безлимит, 2 руб / 30 дней
- Цены меняются только в `tariffs.py`

### Оплата — `/pay`
- Показывает тарифы кнопками с ценами
- Если уже есть активная подписка — предупреждает о смене с подтверждением
- Генерирует платёжную ссылку Продамуса и отправляет пользователю
- Если Продамус не настроен (нет домена/ключей) — показывает «временно недоступно»

### Отписка — `/cancel_sub`
- Показывает подтверждение с датой окончания доступа
- По подтверждению ставит `auto_renew = False`
- Объясняет, что доступ сохраняется до конца периода

### Личный кабинет — `/cabinet`
- Добавлен статус автопродления
- Дата в формате ДД.ММ.ГГГГ
- Для пробного тарифа показывает остаток генераций

### Интеграция Продамуса (`services/prodamus.py`)
- Генерация платёжной ссылки с HMAC-SHA256 подписью
- Проверка подписи входящего вебхука
- Пока нет домена — подпись не проверяется (тестовый режим)

### FastAPI-сервер (`services/webhook.py`)
- Эндпоинт `POST /webhook/prodamus` — принимает уведомления об оплате
- Проверяет подпись, обновляет подписку в БД, уведомляет пользователя
- Эндпоинт `GET /health` для мониторинга
- Запускается параллельно с ботом в одном asyncio event loop

### Новые переменные окружения (`.env`)
```
PRODAMUS_SECRET_KEY=   # секрет из личного кабинета Продамуса
WEBHOOK_URL=           # https://your-domain.ru
WEBHOOK_PORT=8080      # порт FastAPI
```

### Зависимости (`requirements.txt`)
- Добавлены: `fastapi==0.115.0`, `uvicorn==0.30.6`, `python-multipart==0.0.9`

---

## Что нужно сделать после получения домена и ключей

1. Заполнить в `.env` на сервере:
   ```
   PRODAMUS_SECRET_KEY=<ключ из личного кабинета Продамуса>
   WEBHOOK_URL=https://your-domain.ru
   ```
2. Настроить Nginx как reverse proxy на порт 8080
3. Установить SSL (Let's Encrypt + certbot)
4. В личном кабинете Продамуса указать URL вебхука: `https://your-domain.ru/webhook/prodamus`
5. Сделать тестовый платёж (1 руб.) и проверить активацию подписки
6. Установить новые зависимости на сервере: `pip install -r requirements.txt`

---

## Этап 3 — В работе 🔄

### 1. Бэкапы на S3 (Beget) ✅

#### Что сделано
- `utils/backup.py` — скрипт бэкапа БД на S3 Beget:
  - Загружает `bot.db` в бакет с именем `backups/bot_YYYY-MM-DD.db`
  - Политика ротации:
    - Последние 2 месяца — все бэкапы хранятся полностью
    - Старше 2 месяцев — оставляем только первый и последний день каждого месяца, остальные удаляются
- `requirements.txt`: добавлен `boto3==1.35.0`
- `.env.example`: добавлены переменные `S3_ENDPOINT`, `S3_BUCKET`, `S3_ACCESS_KEY`, `S3_SECRET_KEY`, `S3_PREFIX`

#### Настройка на сервере
1. Установить зависимости:
   ```bash
   pip install -r requirements.txt
   ```
2. Добавить в `.env` на сервере:
   ```
   S3_ENDPOINT=https://s3.ru1.storage.beget.cloud
   S3_BUCKET=cbfe50c1dad7-bot-pedagog
   S3_ACCESS_KEY=<ваш ключ>
   S3_SECRET_KEY=<ваш секрет>
   S3_PREFIX=backups/
   ```
3. Настроить crontab (бэкап каждый день в 03:00):
   ```bash
   crontab -e
   ```
   Добавить строку:
   ```
   0 3 * * * cd /home/bot/teacher_bot && /home/bot/teacher_bot/venv/bin/python3 utils/backup.py >> /home/bot/backups/backup.log 2>&1
   ```
4. Проверить вручную:
   ```bash
   cd /home/bot/teacher_bot && python3 utils/backup.py
   ```

### 2. Бот для мессенджера MAX
- Отдельный проект (`max_bot/`) на базе библиотеки `maxapi` или `umaxbot`
- Переиспользование: `database/`, `services/`, `config.py`, `prompt.txt`
- Переписать: `bot.py`, `handlers/`, `keyboards/`
- Отдельная БД (`max_bot.db`)

### 3. Поддержка нескольких админов из `.env`
- `ADMIN_TG_ID=id1,id2,id3` — список через запятую
- Изменения в `config.py` и `handlers/start.py`

### 4. Бот для ВКонтакте

#### 4.1. Подготовка
- Создать сообщество ВКонтакте (тип «Бизнес» или «Тематическое»)
- В настройках сообщества включить «Сообщения сообщества» и «Возможности ботов»
- Работа с API → Создать ключ с правами: «Сообщения», «Управление»
- Включить Long Poll API, выбрать события: новое сообщение, нажатие callback-кнопки
- Добавить токен сообщества в `.env` как `VK_GROUP_TOKEN`

#### 4.2. Структура проекта
```
vk_bot/
├── bot.py           # точка входа (vkbottle)
├── config.py        # загрузка .env (копия с добавлением VK_GROUP_TOKEN)
├── states.py        # FSM-состояния (копия из teacher_bot)
├── prompt.txt       # симлинк или копия из teacher_bot
├── .env
├── database/
│   ├── db.py        # без изменений
│   ├── models.py    # без изменений (tg_id переименовать в user_id)
│   └── queries.py   # без изменений (tg_id → user_id)
├── handlers/
│   ├── start.py     # переписать под vkbottle
│   ├── dialog.py    # переписать под vkbottle
│   ├── generate.py  # переписать под vkbottle
│   ├── cabinet.py   # переписать под vkbottle
│   └── admin.py     # переписать под vkbottle
├── keyboards/
│   └── kb.py        # VK JSON-клавиатуры вместо aiogram InlineKeyboard
├── services/
│   ├── ai_service.py   # без изменений
│   └── scheduler.py    # адаптировать отправку сообщений через VK API
└── utils/
    └── exports.py   # без изменений
```

#### 4.3. Что переиспользуется без изменений (~70%)
- `database/db.py` — подключение к SQLite
- `database/models.py` — создание таблиц
- `database/queries.py` — все запросы к БД
- `services/ai_service.py` — запросы к polza.ai
- `prompt.txt` — системный промпт
- `utils/exports.py` — экспорт CSV

#### 4.4. Что нужно переписать (~30%)
- `bot.py` — заменить `aiogram.Bot` + `Dispatcher` на `vkbottle.Bot`
- Все файлы в `handlers/` — новые декораторы, объекты `Message`, методы отправки
- `keyboards/` — VK использует JSON-формат клавиатур (`KeyboardColor`, `Callback`, `Text`)
- `scheduler.py` — заменить `bot.send_message(tg_id, ...)` на `bot.api.messages.send(user_id, ...)`
- `states.py` — vkbottle имеет свой FSM, адаптировать стейты

#### 4.5. Отличия VK от Telegram
- Идентификация пользователей: `vk_user_id` вместо `tg_id`
- Кнопки: поддерживаются inline (callback) и обычные, формат JSON
- Редактирование сообщений с кнопками: ограничено (отправлять новое сообщение)
- Нет встроенного `/command` — команды обрабатываются как обычный текст
- Long Poll вместо Webhook (можно и Webhook, но Long Poll проще)
- Бот = сообщество, пользователь пишет в «сообщения сообщества»

#### 4.6. Библиотеки
- `vkbottle` — основной фреймворк (асинхронный, роутеры, FSM, мидлвари)
- `aiosqlite` — та же БД
- `openai` — тот же AI-сервис
- `apscheduler` — тот же планировщик

#### 4.7. Отдельная база данных
- Файл: `vk_bot.db`
- Та же структура таблиц, но пользователи идентифицируются по `vk_user_id`
- Данные между Telegram и VK ботами не пересекаются

#### 4.8. Переменные окружения (.env)
| Переменная | Описание |
|---|---|
| VK_GROUP_TOKEN | Токен сообщества ВКонтакте |
| POLZA_API_KEY | API-ключ polza.ai (общий) |
| ADMIN_VK_ID | VK ID администратора |
| DB_PATH | Путь к базе данных (`vk_bot.db`) |
| TZ | Часовой пояс |
| BALANCE_THRESHOLD | Порог баланса polza.ai |
